---
import { legal, legalIds, localizedPath, ui, type Locale } from '../data/site';
import { generatedImageSrc, LOGO_IMAGE } from '../lib/responsive-images';
const { lang } = Astro.props as { lang: Locale };
const t = ui[lang];
const description = {
  tr:'TÃ¼rkiye merkezli stratejik tedarik, doÄŸrulanmÄ±ÅŸ ticari Ã¼rÃ¼nler ve uluslararasÄ± ticari karar desteÄŸi.',
  en:'Strategic sourcing, verified trade products and international commercial decision support from TÃ¼rkiye.',
  de:'Strategische Beschaffung, verifizierte Handelsprodukte und internationale kaufmÃ¤nnische EntscheidungsunterstÃ¼tzung aus TÃ¼rkiye.',
  it:'Sourcing strategico, prodotti commerciali verificati e supporto alle decisioni internazionali dalla TÃ¼rkiye.',
  fr:'Sourcing stratÃ©gique, produits commerciaux vÃ©rifiÃ©s et aide Ã  la dÃ©cision internationale depuis la TÃ¼rkiye.'
}[lang];
---
<footer class="site-footer">
  <div class="container footer-main">
    <div class="footer-brand">
      <a class="brand" href={localizedPath(lang, 'home')}><img src={generatedImageSrc(LOGO_IMAGE,320)} width="320" height="103" alt="CTSEG" loading="lazy" decoding="async" /></a>
      <p>{description}</p>
    </div>
    <nav aria-label={t.products}>
      <strong>{t.products}</strong>
      <a href={localizedPath(lang, 'products')}>{t.allProducts}</a>
      <a href={localizedPath(lang, 'markets')}>{t.markets}</a>
      <a href={localizedPath(lang, 'insights')}>{t.insights}</a>
    </nav>
    <nav aria-label={t.contact}>
      <strong>CTSEG</strong>
      <a href={localizedPath(lang, 'about')}>{t.about}</a>
      <a href={localizedPath(lang, 'contact')}>{t.contact}</a>
      <a href="mailto:info@ctseg.com.tr">info@ctseg.com.tr</a>
    </nav>
  </div>
  <div class="container footer-bottom">
    <p>Â© {new Date().getFullYear()} CTSEG. {t.rights}</p>
    <nav aria-label="Legal">
      {legalIds.map((id) => <a href={localizedPath(lang, 'legal', id)}>{legal[id].titles[lang]}</a>)}
    </nav>
  </div>
</footer>
