---
const { title, steps } = Astro.props as { title?:string; steps:string[] };
---
<section class="process-section section">
  <div class="container">
    {title && <h2 class="section-title">{title}</h2>}
    <ol class="process-rail">
      {steps.map((step) => <li><strong>{step}</strong></li>)}
    </ol>
  </div>
</section>
