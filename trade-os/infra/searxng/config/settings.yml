use_default_settings: true

general:
  debug: false
  instance_name: "CTSEG Search"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  secret_key: "replace-via-SEARXNG_SECRET"
  limiter: false
  public_instance: false
  image_proxy: false
